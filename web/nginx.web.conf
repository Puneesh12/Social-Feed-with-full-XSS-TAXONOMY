server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Static assets (includes /assets/pages.css for the server-rendered pages).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
